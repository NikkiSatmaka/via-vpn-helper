# VIA VPN Helper

#### 1. Start session
```bash
via-cli session start
```
#### 2. Load profile
```bash
via-cli profile load --gateway <vpn_gateway> --username <username> --userpass <password>
```
#### 3. Connect to vpn
```bash
./via-vpn-connect.sh
```
